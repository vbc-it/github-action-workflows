---
- name: Verify
  hosts: all
  tasks:
    - name: Check sample service marker
      ansible.builtin.stat:
        path: /tmp/sample_service_config
      register: sample_service_marker

    - name: Assert sample service marker exists
      ansible.builtin.assert:
        that:
          - sample_service_marker.stat.exists
