---
- name: Create marker file if not present
  ansible.builtin.file:
    path: "/tmp/_installed{{ role2_suffix | default('') }}"
    state: touch
    mode: '0644'
    modification_time: preserve
    access_time: preserve
