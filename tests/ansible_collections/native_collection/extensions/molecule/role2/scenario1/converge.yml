---
- name: Converge
  hosts: all
  gather_facts: true
  tasks:
    - name: "Include the role dynamically"
      ansible.builtin.include_role:
        name: "role2"
      vars:
        role2_suffix: "_role2_scenario1"
